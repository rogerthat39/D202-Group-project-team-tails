<Page x:Class="Assignment.StudyPlanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Assignment"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="700"
      Title="Study Plan">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Width="120" HorizontalAlignment="Left" Margin="10" Click="goToMainPage">Back to main page</Button>

        <Button Grid.Row="1" Width="120" HorizontalAlignment="Left" Margin="10" Click="recommendPapers">Recommend papers</Button>
        <Button Grid.Row="1" Width="120" HorizontalAlignment="Right" Margin="10" Click="createStudyPlan">Create Study Plan</Button>
        <DataGrid Grid.Row="2" x:Name="datagrid"/>
        <Button Grid.Row="3" Width="150" HorizontalAlignment="Right" Margin="10" Click="submitStudyPlan">Check and Save Study Plan</Button>
    </Grid>
</Page>
